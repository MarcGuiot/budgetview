<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.budgetview.android"
          android:versionCode="1"
          android:versionName="1.0"
          android:installLocation="auto"
  >

  <uses-sdk android:minSdkVersion="8"
            android:targetSdkVersion="11"/>
  <uses-permission android:name="android.permission.INTERNET"/>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
  <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

  <application android:label="@string/app_name"
               android:name=".App"
               android:icon="@drawable/budgetview"
               android:theme="@android:style/Theme.NoTitleBar"
    >

    <activity android:name=".HomeActivity"
              android:label="@string/app_name"
              android:theme="@android:style/Theme.NoTitleBar"
      >
      <intent-filter>
        <action android:name="android.intent.action.MAIN"/>
        <category android:name="android.intent.category.LAUNCHER"/>
      </intent-filter>
    </activity>

    <activity android:name=".LoginActivity"
              android:label="BudgetView"
              android:theme="@android:style/Theme.NoTitleBar"
      />
    <activity android:name=".BudgetOverviewActivity"
              android:label="BudgetView"
              android:theme="@android:style/Theme.NoTitleBar"
      />
    <activity android:name=".SeriesListActivity"
              android:label="BudgetView"
              android:theme="@android:style/Theme.NoTitleBar"
      />
    <activity android:name=".TransactionListActivity"
              android:label="BudgetView"
              android:theme="@android:style/Theme.NoTitleBar"
      />
    <activity android:name=".TransactionPageActivity"
              android:label="BudgetView"
              android:theme="@android:style/Theme.NoTitleBar"
      />
    <activity android:name=".DemoActivity"
              android:label="BudgetView"
              android:theme="@android:style/Theme.NoTitleBar"
      />
  </application>
</manifest>
