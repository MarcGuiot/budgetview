- pouvoir creer les composants via l'XML, et les recuperer apres (meme mode operatoire que Mantisse)
  ==> quand on voit passer un id, soit on le recupere dans le contexte, soit on le cree et on le met
      dans le contexte

- proposer un tag pour les principaux composants Swing, en donnant acces aux proprietes principales:
  * table (showHorizontalLines, etc.)
  * tree (expandsSelectedPaths rootVisible rowHeight scrollsOnExpand etc.)
  * list
  * button
  * textField

- ajouter un scroll sur les composants qui le supportent (<table scroll='both' .../>)

- utiliser un resource bundle pour les textes (i18N de l'interface)
  => Splits.setResources(ResourceBundle) ?

- utiliser ColorService pour les couleurs (background="$frame.bg")

- API Splits.show(toto.xml) qui affiche automatiquement la frame.
  * tag <frame>,
  * tag <dialog modal='true'>

- enregistrer des "Action" dans le contexte
  * declaration de menus
  * actions pour les boutons (<button name="..." action="< identifiant de l'action >" />)

- gerer les Border :

  <bevelBorder type="raised">
     ... n'importe quel composant ...
  </bevelBorder>
