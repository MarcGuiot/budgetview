package com.budgetview.gui.components.charts.stack.utils;

import com.budgetview.gui.components.charts.stack.StackChartListener;
import org.globsframework.model.Key;

public class StackChartAdapter implements StackChartListener {
  public void processClick(Key selectedKey, boolean expandSelection) {
  }

  public void rolloverUpdated(Key key) {
  }

  public void processRightClick(Key currentRollover, boolean expandSelection) {
  }
}
